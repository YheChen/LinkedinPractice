import { Placeholder } from "@/components/shell/Placeholder";

export default function ArchivePage() {
  return (
    <Placeholder title="Archive" milestone="Milestone 9 · Archive & persistence">
      Calendar view, filters (game / date / difficulty / completion / best time), random-unsolved,
      and shareable seeded URLs arrive here. We build an <em>original</em> archive from launch —
      we never scrape LinkedIn puzzles (see docs/SPEC.md §Legal).
    </Placeholder>
  );
}
